const config = {
  domainName: 'sonara.ie',
  appName: 'Sonara',
  appDescription:
    'Sonara is an AI-powered voice agent that answers calls 24/7, qualifies leads,books appointments and ensures your business never misses a customer.',
};

export default config;
